const std = @import("std");
const stdio = @import("stdio");
const manifest = @import("../manifest.zig");


pub fn Rm(io: std.Io, allocator: std.mem.Allocator, dir: std.Io.Dir, git_url: []const u8, console: *stdio.Console) !void{
    
}